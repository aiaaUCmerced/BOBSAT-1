This text file is just going to be a massive yap seshion during the coding and development
process. There is no particular order to this, and callbacks will be refered to [x]

Due to github being annoying, i shall do all of this work in a separate github, and then upon 
completing will do a write up on how to setup the key files and generate the binaries and what not


For the most part see https://nasa.github.io/fprime/ and get gud at coding ig.

Installed f-prime bootstrap project into /src
Project name : Bobsat-1 
Namespace : Bobsat1



A git repository has been initialized and F´ has been added as a
submodule, you can now create your first commit.

Get started with your F´ project:

-- Remember to always activate the virtual environment --
cd Bobsat-1
. fprime-venv/bin/activate

-- Create a new component --
fprime-util new --component

-- Create a new deployment --
fprime-util new --deployment

Be sure to run : fprime-util generate && fprime-util build 
this makes the Comnponents portion where all the components go

Im actually troll gotta do cd Bobsat1/Components to make them smh

First gonna focus on 2 components, OBCS and Mag Torquers

Goal: 
    1. Turn on mag torquers
    2. Log data


For now making two components OBCS and Magnetorquers (using the fprime new component comand above)

Basic understanding, the fpp file is the high level overview and auto generating 
part of the code.

Add function SAY_HI per the hello world instructions
fprime-util impl # Converts the high level to low level .cpp

mv template.cpp and template.hpp to regular .cpp and .hpp

Per hello world, copied template .cpp SAY_HI_cmdHandler into 

fprime-util new --deployment # idk what exactly this does atm

Whatever names the deployment : cd DeploymentNamespace
Follow readme in deployment

Ideally make one testing deployment, and anytime you wanna update the component just do fprime-util build within the deployment 

upon doing a review, make a new review deployment


Need to add to the Test deployment and update the instances and topology fpp file to add certain commands and what not
instances :  instance Magnetorqers: Magnetorqers base id 0x10005000 \
    queue size Default.QUEUE_SIZE \
    stack size Default.STACK_SIZE \
    priority 50

topology : instance ComponentNamespaceName

Upon adding them, rebuild it, yada yada. Also note some funky stuff 
with name spaces and topology but whomever is reading my rant im sure
can figure out the kinks just make ur own mini project and add some stuff ot it
and what not 


Here is the basic flowdown of instructions imma have

okayyyy this took way to long but basically.

to geenrate the environment for the fprime, do this pull the github

fprime-bootstrap project --no-generate
Bobsat




